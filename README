Heavy lifting happens in app/models/sprint.rb. Be sure to enter your ScrumWorks credentials in there.

Run with rails runner 'Sprint.accumulate' (or check config/schedule.rb and use whenever to create cron job)
